[pytest]
# `./scripts/test -q` (flags but no path) makes pytest collect from the repo
# root, which walks the frozen runtime copies under data/validation/*/runtime*
# and fails with "import file mismatch" on their duplicate test basenames --
# an environment error that reads like the suite is broken.
testpaths = tests
norecursedirs = data .venv .git node_modules
